// android/build.gradle.kts (project-level)

plugins {
    // Versions are defined in settings.gradle.kts; we just declare them here if needed per older setups.
    // If you prefer, you can leave this block empty or omit this file altogether.
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
